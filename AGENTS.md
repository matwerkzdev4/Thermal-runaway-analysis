# Project Instructions

- At the beginning of work in this project, read `SESSION_LOG.md` if it exists.
- Treat `SESSION_LOG.md` as the concise handoff record for project context, decisions, current state, and next steps.
- If `SESSION_LOG.md` is missing, continue normally. Create or update it when the user asks to end the session, update the log, prepare a handoff, or use the `end-session` skill.
- Keep session log entries short, factual, and written in simple English for context-window efficiency.
- Do not use workbook columns `B:E` in `Test Results.xlsm` for timing analysis; use raw timestamp column `A: Time` and thermocouple columns only.
